{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "request_definitions/GET.schema",
    "title": "GET",
    "description": "A GET request.",
    "type": "object",
    "properties":
    {
        "GET_activate_account":
        {
            "description": "Activate a previously requested account.",
            "$ref": "request_definitions/templates/GET_activate_account.schema"
        },
        "GET_retrieve_available_schema":
        {
            "description": "The template for seeing what schema are available on the server to validate against.",
            "$ref": "request_definitions/templates/GET_retrieve_available_schema.schema"
        }
    }
}
